ContactDirectory\PersonBundle\Model\PersonModel:
    getters:
        firstName:
            - NotBlank:
            - Length:
                  min: 3
                  max: 50
                  minMessage: "Your first name must be at least {{ limit }} characters long"
                  maxMessage: "Your first name cannot be longer than {{ limit }} characters long"
        lastName:
            - NotBlank:
            - Length:
                  min: 3
                  max: 50
                  minMessage: "Your first name must be at least {{ limit }} characters long"
                  maxMessage: "Your first name cannot be longer than {{ limit }} characters long"